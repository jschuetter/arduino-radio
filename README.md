# arduino-radio

Script for running a simple radio using an Arduino, a TEA5767 FM receiver, and a SSD1306 display.



`libraries/` directory provided here for reference. Note that you will need to move it into the parent `/Arduino/` directory to run properly.

